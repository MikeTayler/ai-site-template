/**
 * Client-site image system (see PROJECT.md). Implementation: `./siteImage.tsx`.
 */
export {
  SiteImage,
  type ImageConfig,
  type SiteImageProps,
  type SiteImageSizeContext,
} from "./siteImage";
